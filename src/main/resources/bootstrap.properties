# Setup configuration service details
# Only use if your defining the Registry, not required with Eureka
spring.application.name=incident-manager-service
spring.cloud.config.uri=${LOCAL_CONFIG_SERVER}
